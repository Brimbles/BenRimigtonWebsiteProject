from django.contrib import admin
# from .models import Order


# Register your models here.


#The below class defines some handy defaults within the admin pages
# class OrderAdmin(admin.ModelAdmin):
#     list_display = ('product_category', 'payment_method', 'shipping_cost','unit_price') #Adds extra columns to the list view
#     list_filter = ("product_category",) #adds a filter to the right hand side
#     search_fields = ['product_category', 'payment_method'] #adds a search function


# admin.site.register(Order,OrderAdmin)